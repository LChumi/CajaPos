server:
  port: 8080
spring:
  application:
    name: pos-client

logging:
  level:
    root: info;
    com:
      cumple:
        pos: debug
  file:
    name: C:\\Pos\\logs\\pos.log

eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.110.47:8761/eureka/
  instance:
    hostname: localhost